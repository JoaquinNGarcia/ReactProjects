import React from 'react';
import {

} from 'prop-types';
import {
    NotFoundContainer
} from './styled';

const NotFound = () => {
    return (
        <NotFoundContainer>
            Not Found
        </NotFoundContainer>
    );
};

// NotFound.propTypes = {
// };

export default NotFound;
